--[[ made by #nah2959-ai
    Silent Aim & Anti Aim GUI - Roblox Lua
    Paste this into your executor!
    Educational/learning use only.
]]

-- Simple GUI utility
local function Create(label, kind)
    local inst = Instance.new(kind)
    inst.Name = label
    return inst
end

-- Settings
getgenv().SilentAim_Enabled = false
getgenv().AntiAim_Enabled = false
getgenv().SilentAim_FOV = 300
getgenv().AntiAim_Strength = 10

-- GUI
local ScreenGui = Create("SAntiA-GUI", "ScreenGui")
pcall(function() ScreenGui.Parent = game.CoreGui end)
local Frame = Create("MainFrame", "Frame")
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.new(0,0,0)
Frame.Position = UDim2.new(0.05,0,0.4,0)
Frame.Size = UDim2.new(0,250,0,180)
Frame.Active = true
Frame.Draggable = true
Frame.BorderSizePixel = 0

local title = Create("TitleLbl", "TextLabel")
title.Parent = Frame
title.Text = "Silent Aim & Anti Aim"
title.Size = UDim2.new(1,0,0,30)
title.BackgroundColor3 = Color3.fromRGB(50,50,60)
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 20

-- Silent Aim Toggle
local SA_Toggle = Create("SAToggle", "TextButton")
SA_Toggle.Parent = Frame
SA_Toggle.Position = UDim2.new(0,10,0,40)
SA_Toggle.Size = UDim2.new(0,110,0,30)
SA_Toggle.BackgroundColor3 = Color3.fromRGB(60,80,60)
SA_Toggle.TextColor3 = Color3.new(1,1,1)
SA_Toggle.Text = "Silent Aim: OFF"

SA_Toggle.MouseButton1Click:Connect(function()
    getgenv().SilentAim_Enabled = not getgenv().SilentAim_Enabled
    SA_Toggle.Text = "Silent Aim: "..(getgenv().SilentAim_Enabled and "ON" or "OFF")
end)

-- Silent Aim FOV Slider
local SA_FOV = Create("SAFov", "TextBox")
SA_FOV.Parent = Frame
SA_FOV.Position = UDim2.new(0,130,0,40)
SA_FOV.Size = UDim2.new(0,100,0,30)
SA_FOV.BackgroundColor3 = Color3.fromRGB(40,40,60)
SA_FOV.TextColor3 = Color3.new(1,1,0.7)
SA_FOV.Text = "FOV: "..tostring(getgenv().SilentAim_FOV)
SA_FOV.ClearTextOnFocus = false

SA_FOV.FocusLost:Connect(function()
    local val = tonumber(SA_FOV.Text:match("%d+"))
    if val then
        getgenv().SilentAim_FOV = val
        SA_FOV.Text = "FOV: "..tostring(val)
    else
        SA_FOV.Text = "FOV: "..tostring(getgenv().SilentAim_FOV)
    end
end)

-- Anti Aim Toggle
local AA_Toggle = Create("AAToggle", "TextButton")
AA_Toggle.Parent = Frame
AA_Toggle.Position = UDim2.new(0,10,0,80)
AA_Toggle.Size = UDim2.new(0,110,0,30)
AA_Toggle.BackgroundColor3 = Color3.fromRGB(100,40,40)
AA_Toggle.TextColor3 = Color3.new(1,1,1)
AA_Toggle.Text = "Anti Aim: OFF"

AA_Toggle.MouseButton1Click:Connect(function()
    getgenv().AntiAim_Enabled = not getgenv().AntiAim_Enabled
    AA_Toggle.Text = "Anti Aim: "..(getgenv().AntiAim_Enabled and "ON" or "OFF")
end)

-- Anti Aim Strength box
local AA_ST = Create("AASt", "TextBox")
AA_ST.Parent = Frame
AA_ST.Position = UDim2.new(0,130,0,80)
AA_ST.Size = UDim2.new(0,100,0,30)
AA_ST.BackgroundColor3 = Color3.fromRGB(50,30,30)
AA_ST.TextColor3 = Color3.new(1,0.8,0.7)
AA_ST.Text = "Strength: "..tostring(getgenv().AntiAim_Strength)
AA_ST.ClearTextOnFocus = false

AA_ST.FocusLost:Connect(function()
    local val = tonumber(AA_ST.Text:match("%d+"))
    if val then
        getgenv().AntiAim_Strength = val
        AA_ST.Text = "Strength: "..tostring(val)
    else
        AA_ST.Text = "Strength: "..tostring(getgenv().AntiAim_Strength)
    end
end)

-- Credits Label
local cred = Create("Cred", "TextLabel")
cred.Parent = Frame
cred.Position = UDim2.new(0,0,1,-22)
cred.Size = UDim2.new(1,0,0,22)
cred.BackgroundTransparency = 1
cred.TextColor3 = Color3.fromRGB(170,170,170)
cred.Text = "Copilot - GUI Demo"
cred.Font = Enum.Font.SourceSans
cred.TextSize = 16

--------------------------------------------------------
-- Actual Script Logic
--------------------------------------------------------

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- PART 1: Silent Aim (with FOV and toggling)
local function getClosestPlayer()
    if not getgenv().SilentAim_Enabled then return nil end
    local closest, shortest = nil, getgenv().SilentAim_FOV or 300
    local mousePos = UIS:GetMouseLocation()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
            local head = p.Character.Head
            local screenPos, onScreen = Camera:WorldToViewportPoint(head.Position)
            if onScreen then
                local diff = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                if diff < shortest then
                    closest, shortest = p, diff
                end
            end
        end
    end
    return closest
end

if not getgenv().SilentAimHooked then
    getgenv().SilentAimHooked = true
    local rawMeta = getrawmetatable(game)
    local oldNamecall = rawMeta.__namecall
    setreadonly(rawMeta, false)

    rawMeta.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        local args = {...}

        if method == "FireServer" and tostring(self):lower():find("hit") then
            local target = getClosestPlayer()
            if target and target.Character and target.Character:FindFirstChild("Head") then
                args[2] = target.Character.Head.Position
                return oldNamecall(self, unpack(args))
            end
        end
        return oldNamecall(self, ...)
    end)
    setreadonly(rawMeta, true)
end

-- PART 2: Anti-Aim (toggle and strength adjustable)
spawn(function()
    local last = 0
    local rs = game:GetService("RunService")
    while true do
        rs.RenderStepped:Wait()
        if getgenv().AntiAim_Enabled then
            local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            for _, partName in ipairs({"Head", "UpperTorso", "HumanoidRootPart", "Torso"}) do
                local part = char:FindFirstChild(partName)
                if part and part:IsA("BasePart") then
                    -- Jitter/Spin, strength setting
                    local s = getgenv().AntiAim_Strength or 10
                    part.CFrame = part.CFrame + Vector3.new(
                        math.random(-s,s)/5,
                        math.random(-s,s)/5,
                        math.random(-s,s)/5
                    )
                end
            end
        end
    end
end)
